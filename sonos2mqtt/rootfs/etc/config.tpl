{
  "mqtt": "mqtt://${MQTT_USER}:${MQTT_PASS}@${MQTT_HOST}:${MQTT_PORT}",
  "clientid": "${MQTT_CLIENT_ID}",
  "prefix": "${MQTT_TOPIC_PREFIX}",
  "distinct": ${DISTINCT},
  "device": "${SONOS_DEVICE_IP}",
  "ttslang": "${TTS_LANG}",
  "ttsendpoint": "${TTS_ENDPOINT}",
  "discovery": ${DISCOVERY},
  "discoveryprefix": "${DISCOVERY_PREFIX}",
  "log": "${LOGGING}",
  "friendlynames": "${FRIENDLY_NAMES}"
}
